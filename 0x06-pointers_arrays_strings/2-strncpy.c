#include "main.h"

/**
*_strncpy - copy strings
*@dest: destination
*@src: source
*@n: number of strings to copy
*/

char *_strncpy(char *dest, char *src, int n)
{
	char *p = dest;
	int i;

	for (i = 0; i < n; i++)
	[
		if (src[i] = '\0')
			_putchar('\0');
		dest[i] = src[i];
		
	]
	return (p);
}
