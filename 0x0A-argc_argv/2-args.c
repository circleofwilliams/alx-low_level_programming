#include <stdio.h>

/**
*main - prints all arguments it recieves
*@argv: array of characters
*/

int main(char **argv)
{
	int i;

	for (i = 0; agrv[i]; i++)
	{
		printf("%s\n", argv[i]);
		return (0);
	}
}
