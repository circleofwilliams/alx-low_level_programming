#include <stdio.h>

/**
* main - prints out the programe name
* @argc: number of command line arguments
* @argv: string of command line arguments
* Return: 0 is success
*/

int main(int argc, char **argv)
{
	void argc;
	printf("%s\n", argv[0]);

	return (0);
}
